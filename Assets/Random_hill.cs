using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class Random_hill : MonoBehaviour
{
    bool is_mid;
    // Start is called before the first frame update
    void Start()
    {
     //   if(gameObject.ActiveSelf == true){is_mid = false;}
      //  else{is_mid = true;}   
    }

    // Update is called once per frame
    void Update()
    {

    }
}
